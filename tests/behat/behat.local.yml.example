default:
  extensions:
    Behat\MinkExtension\Extension:
      base_url: 'http://127.0.0.1:8888'
    Drupal\DrupalExtension\Extension:
      drush:
        alias: self
      drupal:
        # Change this to the absolute path to Drupal install.
        drupal_root: "/home/travis/build/smithmilner/drupal"